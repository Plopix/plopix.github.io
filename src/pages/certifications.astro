---
import Award from "../components/award.astro";
import Certification from "../components/certification.astro";
import { me } from "../core/constants";
import { awards } from "../core/awards";
import { certifications } from "../core/certification";
import BaseLayout from "../layouts/base-layout.astro";
---

<BaseLayout
  title="Certifications, Awards & Recognitions"
  description=`Browse ${me.firstName} ${me.lastName}'s certifications, honors, and achievements—industry-recognized credentials in software development, cloud computing, and tech leadership.`
>
  <section>
    <div class="std-page-container">
      <h1
        class="text-2xl font-medium text-balance tracking-tight text-neutral-900 dark:text-white sm:text-4xl"
      >
        Certifications & Honors
      </h1>
      <p class="mt-4 std-description">
        Throughout my career, I've pursued <strong
          >certifications to deepen my expertise</strong
        >
        and <strong
          >earned honors that recognize my contributions and achievements</strong
        > in technology and leadership. From industry-recognized credentials to professional
        awards, each represents a commitment to continuous learning, innovation,
        and excellence. Here's a look at my <strong>
          certifications, honors, and professional recognitions
        </strong>.
      </p>

      <h2
        class="text-xl font-medium text-balance mt-5 tracking-tight text-neutral-900 dark:text-white sm:text-3xl"
      >
        Certifications
      </h2>
      <div x-data="{ showMore: false }" class="space-y-2">
        <ul
          role="list"
          class="grid mt-4 grid-cols-2 gap-2 border-t pt-2 dark:border-neutral-700 lg:col-span-2"
        >
          {
            certifications.slice(0, 8).map((certification, index) => (
              <li class="flow-root">
                <Certification {...certification} index={index} />
              </li>
            ))
          }
        </ul>
        <template x-if="showMore">
          <ul
            role="list"
            class="grid mt-4 grid-cols-2 gap-2 dark:border-neutral-700 lg:col-span-2"
          >
            {
              certifications.slice(8).map((certification, index) => (
                <li class="flow-root">
                  <Certification {...certification} index={6 + index} />
                </li>
              ))
            }
          </ul>
        </template>
        <button
          @click="showMore = !showMore"
          class="text-main-500 hover:underline text-sm font-medium py-2"
        >
          <span
            x-text="showMore ? 'Close legacy certifications' : 'Dig into legacy certifications'"
          ></span>
        </button>
      </div>

      <h2
        class="text-xl font-medium text-balance mt-5 tracking-tight text-neutral-900 dark:text-white sm:text-3xl"
      >
        Honors & Awards
      </h2>

      <ul
        role="list"
        class="grid grid-cols-1 sm:grid-cols-2 gap-x-6 gap-y-10 mt-4 border-t border-neutral-200 dark:border-neutral-700 pt-2"
      >
        {
          awards.map((award) => (
            <li>
              <Award {...award} />
            </li>
          ))
        }
      </ul>

      <h2
        class="text-xl font-medium text-balance mt-5 tracking-tight text-neutral-900 dark:text-white sm:text-3xl"
      >
        Patent
      </h2>

      <div
        class="mt-4 border-t border-neutral-200 dark:border-neutral-700 pt-2"
      >
        <div class="flex-1 h-full flex flex-col">
          <div class="inline-flex space-x-1 items-center">
            <p class="text-xs font-medium text-neutral-900 dark:text-white">
              US U.S. Patent: US20150248394A1
            </p>
            <span aria-hidden="true">&middot;</span>
            <div class="flex text-xs text-neutral-400">
              <span>May 2015</span>
            </div>
          </div>
          <div class="">
            <p class="text-lg font-semibold leading-6 std-paragraph">
              Automatically uploading user profile information
            </p>

            <p
              class="mt-2 text-sm leading-6 text-neutral-500 dark:text-neutral-400 line-clamp-3"
            >
              Techniques for automatically transmitting user profile data over a
              network are provided. In one technique, a browser displays a form
              and a QR code (or other form of encoded data) on a webpage. A
              mobile application scans the QR code, which causes the mobile
              application to identify user profile data (that may be stored
              locally with respect to the mobile application) and send the user
              profile data to a Campaign server. The Campaign server causes the
              form that is displayed by the browser to be filled in with the
              user profile data. The browser than transmits the filled-in form
              to a partner server that provides the webpage. In another
              technique, a mobile application scans a QR code, which may be
              displayed on a flyer, a poster, a computer screen, or a television
              screen. The mobile application identifies and transmits user
              profile data to a Campaign server.
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>
